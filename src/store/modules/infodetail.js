const state = {
  ResetFormEdit: ""
}

const getters = {
  
}

const mutations = {
  SET_EDITOR(state,value) {
    return state.ResetFormEdit = value;
  }
}

const actions = {
  
}

export default {
  namespaced: true,
  state,
  getters,
  mutations,
  actions
}
